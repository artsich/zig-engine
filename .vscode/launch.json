{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug",
            "program": "${workspaceFolder}/zig-out/bin/zig-engine.exe", 
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "build" 
        }
    ]
}
